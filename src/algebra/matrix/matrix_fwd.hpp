// Created by Franz Seckel on 19.12.2025.
#pragma once
#include <cstddef>

namespace fs::math::algebra
{
    /// Forward-declaration for the matrix type
    template<typename valueType, std::size_t Rows, std::size_t Columns>
    struct Matrix;

} // namespace fs::math::algebra
